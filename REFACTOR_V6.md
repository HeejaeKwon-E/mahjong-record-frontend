# Refactor V6

## 반영한 피드백

1. 전체 통계에서 랭킹 비교 그래프와 랭킹 카드를 제거했습니다.
2. 접혀 있던 전체 데이터 표를 통계 화면의 기본 콘텐츠로 변경했습니다.
3. 헤더 날짜를 눌러도 달력이 열리지 않을 수 있던 브라우저 호환 문제를 수정했습니다.

## 변경 파일과 이유

### `src/components/tab/allStatsTab/AllTimeStatsSection.tsx`

- Recharts `BarChart`와 지표 전환 상태 제거
- 랭킹 카드 제거
- `전체 데이터` 펼치기 상태 제거
- 기간 필터 아래에 전체 통계 표를 항상 표시
- 기존 열 정렬과 플레이어 상세 진입 기능 유지
- 모바일에서는 첫 번째 플레이어 열을 고정하고 가로 스크롤 제공
- 0게임 플레이어가 평균순위/비율 정렬의 최상단에 표시되지 않도록 정렬 보정

전체 통계 데이터 흐름은 다음처럼 단순해졌습니다.

```text
GET /api/stats/all?start_date=&end_date=
  -> AllPlayerTotalStats[]
  -> 선택 열 기준 정렬
  -> 전체 데이터 표
  -> 플레이어 이름 클릭
  -> 개인 통계 상세
```

개인 통계 상세의 월별 평균순위 그래프는 플레이어 한 명의 추세를 확인하는
용도이므로 그대로 유지했습니다.

### `src/pages/MahjongPage.tsx`

기존 헤더 날짜는 투명한 `<input type="date">`를 날짜 표시 영역에 덮어놓고
브라우저가 클릭만으로 달력을 열어주기를 기대했습니다. 일부 Chrome/Safari에서는
투명한 날짜 입력 클릭이 달력을 열지 않고 포커스만 이동할 수 있었습니다.

수정 후 흐름:

```text
중앙 날짜 버튼 클릭
  -> dateInputRef.current.showPicker()
  -> 미지원/실패 시 input.click() 폴백
  -> 날짜 선택
  -> handleDateChange()
  -> selectedDateAtom 갱신
  -> useServerSync가 해당 날짜 데이터 재조회
```

좌우 하루 이동 버튼과 오늘만 라운드 저장할 수 있는 기존 정책은 변경하지 않았습니다.

## 빌드 결과

```text
초기 JS                 250.87 kB (gzip 79.83 kB)
전체 통계 청크           15.10 kB (gzip 4.96 kB)
나니키루 청크           209.95 kB (gzip 48.34 kB)
공용 LineChart 청크     359.17 kB (gzip 105.52 kB)
```

500KB 초과 청크 경고는 발생하지 않습니다.

## 실행 및 검증

```bash
cd mahjong-stat-front
npm install
npm run build
npm run lint
npm run dev
```

화면 확인:

1. 전체 탭 진입 시 기간 필터 아래에 표가 즉시 표시되는지
2. 랭킹 비교 지표 버튼, 그래프, 랭킹 카드가 제거됐는지
3. 평균순위, 1위율, 연대율, 4위율 및 등수 횟수가 모두 표시되는지
4. 각 열 제목을 누르면 오름차순/내림차순 정렬되는지
5. 0게임 플레이어가 평균순위 정렬의 최상단에 오지 않는지
6. 플레이어 이름을 누르면 개인 상세 화면이 열리는지
7. 모바일에서 플레이어 열이 고정되고 표를 가로로 밀 수 있는지
8. 헤더 중앙 날짜를 누르면 날짜 선택기가 열리는지
9. 선택한 날짜 기준으로 일별 데이터와 나니키루 문제가 갱신되는지
10. 과거 날짜에서는 기존처럼 라운드 저장이 차단되는지

## 검증 결과

- `npm run build`: 성공
- `npm run lint`: 성공
- Vite 500KB 초과 청크 경고: 없음
- 백엔드 및 API 변경: 없음
