# Refactor V7

이번 변경은 기록 탭과 일별 탭의 연결성을 높이는 세 가지 프론트엔드 피드백을 반영합니다. 백엔드 API와 저장 데이터 형식은 변경하지 않았습니다.

## 변경 파일과 이유

### `src/components/tab/recordTab/PlayerSection.tsx`

- 오늘의 멤버 영역을 접근 가능한 접기/펼치기 버튼으로 변경했습니다.
- 세션 멤버가 서버에서 복원되면 기본적으로 접힌 상태로 표시합니다.
- 접힌 상태에서도 선택 인원과 이름을 확인할 수 있습니다.
- 멤버가 없는 상태에서는 목록을 펼쳐 바로 선택할 수 있습니다.
- 사용자가 목록에서 멤버를 편집하기 시작하면 선택 도중 자동으로 접히지 않습니다.

### `src/pages/MahjongPage.tsx`

- 날짜가 변경될 때 `PlayerSection`을 새 날짜 기준으로 다시 생성합니다.
- 따라서 접힘 상태가 이전 날짜에서 새 날짜로 잘못 이어지지 않습니다.

### `src/state/mahjongAtoms.ts`

- `statsByPlayerAtom`이 `selectedPlayerIdsAtom`을 함께 읽도록 변경했습니다.
- 선택 멤버가 한 명 이상이면 그 사람들의 일별 통계만 반환합니다.
- 선택 멤버가 없으면 기존처럼 해당 날짜의 전체 통계를 반환합니다.

### `src/components/tab/dailyStatsTab/DailyScoreChart.tsx`

- 누적 벌점 그래프를 라운드별 실제 등수 그래프로 변경했습니다.
- Y축은 `1위 → 4위`이며 1위가 위쪽에 표시됩니다.
- 참가하지 않은 라운드는 `null`로 처리해 잘못된 등수를 연결하지 않습니다.
- 선택 멤버가 있으면 선택한 사람의 선만, 없으면 전체 참가자의 선을 표시합니다.
- 하단 범례의 숫자는 누적 벌점 대신 각 플레이어의 가장 최근 등수입니다.

### `src/components/tab/dailyStatsTab/RoundHistorySection.tsx`

- 선택 멤버가 있으면 그중 한 명 이상이 참가한 라운드만 표시합니다.
- 각 라운드 카드 안에는 선택 멤버의 실제 등수만 표시합니다.
- 삭제는 라운드 전체에 적용되므로 삭제 확인창에서는 네 명 전체를 보여줍니다.

## 데이터 흐름

```text
기록 탭 멤버 선택
  → selectedPlayerIdsAtom
  ├─ statsByPlayerAtom
  │   → 일별 점수 요약 카드
  ├─ DailyScoreChart
  │   → 라운드별 1~4위 그래프
  └─ RoundHistorySection
      → 선택 멤버 관련 라운드와 등수

selectedPlayerIdsAtom이 []
  → 일별 탭 전체 참가자/전체 라운드 표시
```

서버 응답인 `roundsAtom`은 수정하지 않습니다. 화면에 필요한 파생 데이터만 프론트에서 필터링하기 때문에 저장, 삭제, 날짜 이동 API의 호환성은 그대로 유지됩니다.

## 실행 및 검증

```bash
cd mahjong-stat-front
npm install
npm run lint
npm run build
npm run dev
```

확인 항목:

1. 기존 라운드의 멤버가 복원되면 오늘의 멤버 영역이 접혀 있는지
2. 접힌 헤더에 선택된 이름과 `4 / 4`가 표시되는지
3. 멤버가 없는 날짜에서는 목록이 기본적으로 펼쳐지는지
4. 멤버를 새로 선택하는 도중 목록이 자동으로 닫히지 않는지
5. 일별 탭 요약 카드에 선택 멤버만 표시되는지
6. 등수 흐름 Y축이 1~4위이고 1위가 위에 표시되는지
7. 참가하지 않은 라운드에서 해당 플레이어 선이 끊기는지
8. 라운드 기록 카드에 선택 멤버의 실제 등수만 표시되는지
9. 기록 탭에서 멤버를 모두 해제하면 일별 탭 전체 내용이 표시되는지
10. 오늘 라운드 삭제 후 요약, 그래프, 히스토리가 함께 갱신되는지

검증 결과:

- `npm run lint`: 통과
- `npm run build`: 통과
- `go test ./...`: 현재 작업 환경에 Go 실행 파일이 없어 미실행
- 백엔드 디렉터리: V6와 전체 파일 비교 결과 변경 없음
